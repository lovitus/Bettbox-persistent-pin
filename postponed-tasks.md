# Postponed tasks

- Add Windows Authenticode signing after a suitable code-signing certificate or SignPath project is available. Current Windows installers are deliberately published as **unsigned**, and the release notes state that explicitly.
- Renew and rotate the macOS Developer ID certificate before its current expiry date (2027-02-01), then replace the repository secrets without changing the workflow.
